import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#d8649c',
        },
        secondary: {
            main: '#9013fe',
        },
        text: {
            primary: '#000',
            secondary: '#fff',
        }
    },
});

export default theme;
